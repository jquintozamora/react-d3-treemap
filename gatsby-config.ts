import type { GatsbyConfig } from "gatsby"

const config: GatsbyConfig = {
  siteMetadata: {
    title: "React D3 Treemap",
  },
  plugins: [],
  pathPrefix: `/react-d3-treemap`,
}

export default config
